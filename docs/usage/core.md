## Core

Always installed, regardless of which modules a machine has enabled.

- `j <partial-name>` — jump to a frecent directory (zoxide). `j` with no
  argument jumps to the most recent one.
- `g` — short alias for `git status`.
